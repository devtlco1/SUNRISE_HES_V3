/**
 * Built-in role row ids — must match `lib/rbac/seed-defaults` / initial `data/rbac-roles.json`.
 */
export const BUILTIN_ROLE_ID_SET = new Set([
  "role-admin",
  "role-operator",
  "role-viewer",
  "role-support",
  "role-developer",
])
